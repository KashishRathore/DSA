package Trie;

// Q -> Given a string of length n of lowercase alphabet characters, we need to count total no. of distinct substrings of this string.

public class Count_Unique_Substrings {
    // prefix-suffix concept
    // all prefix of all suffix 
    // all suffix of all prefix

    // step 1 -> Find all suffix of string
    // step 2 -> Create a trie from suffix
    // step 3 -> Total node of trie = count of unique prefix
    public static void main(String[] args) {

    }
}
